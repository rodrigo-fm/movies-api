import 'package:flutter/material.dart';

class SearchMoviesResultView extends StatelessWidget {
  const SearchMoviesResultView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
